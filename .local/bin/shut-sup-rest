#!/bin/bash
choices="Lock\nSuspend\nReboot\nShutdown"

chosen=$(echo -e "$choices" | dmenu -i)

case "$chosen" in
	Lock) slock ;;
	Suspend) loginctl suspend && slock ;;
	Reboot) sudo reboot ;;
	Shutdown) sudo shutdown -h now ;;
esac
